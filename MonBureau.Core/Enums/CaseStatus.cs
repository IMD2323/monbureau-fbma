namespace MonBureau.Core.Enums
{
    public enum CaseStatus
    {
        Open = 0,
        InProgress = 1,
        Closed = 2,
        Archived = 3,      // ADD THIS if missing
        OnHold = 4         // ADD THIS if missing
    }
}