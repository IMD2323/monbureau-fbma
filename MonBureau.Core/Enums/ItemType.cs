namespace MonBureau.Core.Enums
{
    public enum ItemType
    {
        Document = 0,
        Hearing = 1,       // ADD THIS if missing
        Expense = 2,
        Note = 3,          // ADD THIS if missing
        Task = 4           // ADD THIS if missing
    }
}